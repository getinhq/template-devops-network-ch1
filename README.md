# Network latency logger

## Task
Write a script to measure network latency to a host (e.g. google.com) and log results for analysis.

## Starter (not finished)
`measure_once()` is a stub. Tests require real latency measurement logic.

## Your work
- Implement ping, TCP connect timing, or `time` around HTTP — document choice.
- Return latency in milliseconds as a float.
- Optional: CLI to append CSV logs.
